{
  "name": "@openclaw/executor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@openclaw/audit-runtime": "workspace:*",
    "@openclaw/db": "workspace:*",
    "@openclaw/domain": "workspace:*",
    "@openclaw/memory-runtime": "workspace:*",
    "@openclaw/os-adapters": "workspace:*",
    "@openclaw/project-profiles": "workspace:*",
    "@openclaw/team-router": "workspace:*",
    "@openclaw/telemetry": "workspace:*"
  }
}
